@model VisitsModel

@{
    ViewData["Title"] = "Dados da visita";
}

<div>
    <form class="form-visit w-50 d-flex">
        <label for="txtAssessment">Informe o relatório sobre a visita</label>
        <textarea class=form-control"/>

    </form>
</div>