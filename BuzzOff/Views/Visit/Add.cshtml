@model VisitsModel

@{
    ViewData["Title"] = "Dados da visita";
}

<div>
    <form asp-controller="Visit" asp-action="Add" method="post" class="form-visit w-50 d-flex">
        <label for="txtAssessment">Informe o relatório sobre a visita</label>
        <textarea class="form-control" rows="6"/>
        <label>É foco?</label>
        <label><input type="checkbox" name="IsFocus">Sim</label>
        <label><input type="checkbox" name="IsFocus">Não</label>
           
    </form>
</div>